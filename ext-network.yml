# =============================================================================
# Ext Network Overlay - Attach xdc to the external Traefik network
# =============================================================================
# Add :ext-network.yml to COMPOSE_FILE in .env to publish xdc via Traefik.
# =============================================================================

networks:
  default:
    name: ${DOCKER_EXT_NETWORK:-traefik_default}
    external: true
